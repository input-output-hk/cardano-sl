const assert = require('chai').assert;

describe('ClientApi', () => {
  describe('first tests [will be removed later on]', () => {
    it('equal', () => {
      assert.equal('oooh yeaaaah', 'oooh yeaaaah');
    })
  })
})
