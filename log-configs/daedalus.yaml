# This config is used by Daedalus (in production).

rotation:
    logLimit: 5242880 # 5MB
    keepFiles: 20
loggerTree:
  severity: Debug+
  syncWalletWorker:
    severity: Error+
  files:
    - pub/node.pub
    - node
