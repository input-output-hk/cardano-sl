# Same as log-template-demo.yaml, but without files,
# ready to use if you're too lazy to use sed to clean up files
# manually.

severity: Debug
termSeverity: Debug
node:
    severity: Debug
    file: [update-node-tmp.log, pub/update-node-tmp.log.pub]
