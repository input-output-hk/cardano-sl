{-# LANGUAGE CPP #-}

-- | Module for exposing JSON instances for Cardano types

module Pos.Wallet.Aeson
       (
       ) where

import           Pos.Wallet.Aeson.ClientTypes ()
import           Pos.Wallet.Aeson.WalletBackup ()
